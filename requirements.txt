# =============================================================================
# Requirements for AI Data Reliability Agent
# =============================================================================
# Install with: pip install -r requirements.txt
# =============================================================================

# DataHub SDK — For metadata emission and DataHub API communication
acryl-datahub>=0.14.0

# Data processing
pandas>=2.0.0

# PostgreSQL adapter (for direct DB schema inspection)
psycopg2-binary>=2.9.0

# Environment variable management
python-dotenv>=1.0.0

# Utilities
requests>=2.31.0

# Web UI — Streamlit frontend dashboard
streamlit>=1.35.0

# Charts and visualization
plotly>=5.0.0

# Multi-agent orchestration — LangGraph state machine for the 5-agent team
langgraph>=0.2.0
langchain-core>=0.3.0

# LLM providers (provider-agnostic narrator picks whichever key is present)
openai>=1.40.0
langchain-openai>=0.2.0

# Optional secondary provider — deterministic fallback covers both if missing
anthropic>=0.34.0
langchain-anthropic>=0.2.0
